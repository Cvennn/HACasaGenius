name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set version number in manifest.json
        run: |
          jq '.version = "${{ github.event.release.tag_name }}"' \
            custom_components/YOUR_DOMAIN/manifest.json > tmp \
            && mv tmp custom_components/YOUR_DOMAIN/manifest.json

      - name: Zip the integration
        run: |
          cd custom_components/YOUR_DOMAIN
          zip -r ../../YOUR_DOMAIN.zip ./

      - name: Upload zip to release
        uses: softprops/action-gh-release@v2
        with:
          files: YOUR_DOMAIN.zip
